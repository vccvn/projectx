import { Injectable } from '@angular/core';
import { AppEditorService } from './app-editor.service';

@Injectable({
    providedIn: 'root'
})
export class AppViewerService extends AppEditorService {
    
}
